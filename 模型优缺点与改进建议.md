# 模型优缺点与改进建议

## 一、说明

本文建立了从确定性计划购电、含预测误差的随机计划，到允许在规定时刻调整购电计划的滚动调度模型。下面的优缺点与改进建议均以现有代码和题目所给信息为依据，不额外假设电池寿命、设备折旧、天气类别等题目没有提供的数据。

这里所说的“改进”并不等于现有模型存在明显错误。有些改进会改变模型研究的层次，有些会引入新的评价标准或管理偏好。对于每项改进，本文同时说明为什么现有模型没有直接采用，避免把未经实现或未经验证的设想说成必然更好的方法。

## 二、模型的主要优点

### 1. 各问题之间具有清楚的递进关系

问题一先在负荷、光伏和电价均已给定的条件下建立线性规划，研究计划购电与储能充放电的基本关系。问题二进一步考虑负荷和光伏预测误差以及紧急购电。问题三允许在新的光伏预测发布后调整剩余购电计划。问题四又将固定电价扩展为随时间变化的电价。

这种递进方式的优点是，每增加一层现实因素，都能够明确看出模型增加了哪些变量、约束和费用，而不是一开始就建立一个难以解释的复杂模型。

### 2. 严格区分“当时已知的信息”和“未来才会知道的信息”

模型在制定和调整购电计划时，只使用当时已经发布的预测以及此前已经发生的历史数据。实际负荷、实际光伏和实际电价只有到相应时段开始时才能进入执行决策，未来真实数据只用于事后结算和回测。

这一处理避免了常见的“用未来数据预测过去”问题。模型得到的策略虽然不一定是知道全年真实数据后的最低费用方案，但它是按照现实信息到达顺序可以执行的方案。

### 3. 对题目中的购电和储能规则刻画较完整

模型分别记录计划购电、上调购电、下调购电、紧急购电、储能充电、储能放电、弃光和储电状态，并按照题目规定计算对应费用。

紧急购电只能弥补当前供电缺口，不能用于给电池充电；已经执行或已经过去的购电承诺不能被后续调整；上调和下调均相对于上一次有效计划逐次结算。这些处理能够避免重复收费、修改历史计划以及利用紧急购电进行储能套利等不符合题意的情况。

### 4. 对不确定性的处理保留了完整日误差的变化形态

问题二及后续模型不是把每个十分钟的误差完全独立地随机生成，而是抽取同一个历史日期的完整误差曲线。这样可以保留误差在一天内的连续变化，也能在一定程度上保留负荷、光伏以及动态电价误差之间原有的联系。

例如，连续多个时段光伏均低于预测的情况不会被拆成彼此无关的零散误差，因此生成的场景比逐时独立抽样更接近实际运行过程。

### 5. 在求解精度与全年计算速度之间进行了明确取舍

问题三和问题 4-3同时保留了两种执行方式。逐十分钟滚动方法会在每个时段重新求解模型，能够充分利用最新状态，但全年需要进行数万次随机混合整数规划求解。事件驱动方法只在 0:00、6:00、12:00 和 18:00 等预测或承诺事件发生时求解完整模型，其他时段按照最近一次优化结果和当前实际数据执行。

现有局部对照表明，事件驱动方法的求解速度约为逐十分钟滚动方法的 9.6—10.7 倍。代码没有将两种方法描述为完全等价，而是同时记录费用、紧急购电量、期末储电量和计算时间的差异。这种处理能够如实展示计算效率与近似误差之间的关系。

### 6. 模型结果具有较完整的独立校验

现有校验不仅检查程序是否成功结束，还重新计算能量平衡、储电状态递推、储能上下限、充放电互斥、购电承诺冻结和各类费用。问题一还使用不同算法交叉求解，并检查最优性条件。

因此，现有验证至少能够说明输出结果与所建立的数学模型和结算规则一致，减少代码运行成功但结果表填写错误、时间列错位或费用重复计算的风险。

## 三、模型的局限与改进方向

### 1. 预测方法主要按照预测误差选择，没有直接按照最终决策效果选择

#### 当前做法及其影响

现有模型比较了前一日同时段、上周同日同时段、最近 7 日均值和最近 4 个同星期日均值等方法，并根据 MAE、RMSE 等预测误差选择“上周同日同时段”方法。现有结果表明，该方法在已比较的方法中误差最低，因此当前选择具有实际依据。

但是，预测误差最小不一定代表最终购电费用最低。某种方法即使整体误差较小，如果它经常在高电价时段低估净负荷，仍可能带来较高的紧急购电费用。另一种方法的平均误差可能略大，但如果它在高费用时段预测得更准确，最终调度结果反而可能更好。

#### 可以怎样改进

后续可以将每一种候选预测方法分别接入同一个购电与储能优化模型，除了比较 MAE 和 RMSE，还比较其产生的总费用、紧急购电量、紧急购电天数以及高电价时段的预测偏差。这样，预测方法的评价标准就能从“数值上预测得准不准”扩展到“是否真正有利于制定购电策略”。

#### 为什么现有模型没有直接采用

现有模型采用的是较清楚的模块化思路：先独立检验预测方法，再把选出的预测结果输入优化模型。这样可以明确区分预测问题和调度问题，也便于解释预测方法本身的准确性。

如果直接按照最终费用选择预测方法，费用差异会同时受到预测、储电初始状态、随机场景和调度策略影响，难以判断改进究竟来自预测本身还是来自后续优化。该改进研究的是“预测与决策联合评价”，比当前的“先预测、再优化”更深入，但必须设计独立的训练期和评价期，不能仅凭同一年度结果选择费用最低的方法后再用同一结果证明其有效。

### 2. 问题二的两阶段计划模型与实际逐时执行过程仍存在差异

#### 当前做法及其影响

问题二在 0:00确定全天共享的计划购电量，然后分别在每条随机场景中安排储能和紧急购电。对于某一条场景，模型在计算储能方案时能够看到这条场景后续完整的日误差曲线。

例如，场景甲表现为“上午缺电、下午光伏充足”，场景乙表现为“上午光伏充足、下午缺电”。在场景甲中，模型知道下午一定能够补充电量，因此上午可能更愿意放电；在场景乙中，模型又知道下午一定会缺电，因此上午可能主动保留电量。

现实运行中的上午并不知道下午最终会出现哪种情况。因此，两阶段场景模型中的储能安排具有一定的理想化特征，可能低估实际运行中的紧急购电费用，并使 0:00计划购电量偏于乐观。现有代码通过因果执行器保证最终调度不使用未来信息，并以实际因果回测费用作为正式结果，但计划模型和执行模型之间仍不是完全相同的信息结构。

#### 可以怎样改进

后续可以建立多阶段随机规划。多阶段模型不会在 0:00就假定模型知道某条场景的完整未来，而是要求：在两个场景尚未被已经发生的信息区分开之前，它们必须采用相同的储能决策；只有获得新的实际信息并确认场景出现差异以后，后续决策才允许分开。

这样可以使计划模型中的信息获得过程更加接近现实，减少场景模型“提前知道未来”的理想化程度。

#### 为什么现有模型没有直接采用

现有数据提供的是若干条完整的历史负荷、光伏和价格曲线，但没有直接提供一棵多阶段场景树。建立场景树还必须进一步确定：在哪些时刻产生分支、哪些历史场景在当前时刻仍然不可区分、观察到何种差异后可以分支、每个后续分支的概率是多少。

这些内容不能从“随机抽取若干完整历史日”中直接得到。如果在缺乏充分信息的情况下人为设定分支时刻、分类标准和概率，多阶段模型虽然形式更复杂，主观性却可能比现有等概率历史抽样更强。因此，题目没有提供足够多的条件来唯一确定多阶段场景树，这正是现阶段不直接采用该模型的重要原因。

计算量也是另一项重要影响。一天包含 144 个十分钟时段，多阶段模型除了需要为各场景设置变量，还要增加大量“相同信息条件下必须作出相同决策”的非前视约束。若允许场景随信息不断分叉，可能路径数会快速增长。该方法不是在现有模型中简单增加一条约束，而是需要重新设计场景生成、分支概率和求解方法。

因此，现有两阶段模型是一种假设较少、能够清楚回答日前计划问题且便于验证的近似方案；多阶段模型适合作为在获得更充分场景转移信息或具备可靠场景树构造方法后的结构性深化。

### 3. 随机场景采用有限历史样本和等概率处理

#### 当前做法及其影响

现有模型主要从最近 28 天的历史误差中抽取 12 条完整误差路径，并通常给予相同概率。这种方法不需要人为判断某一天比另一天重要，具有简单、中立和容易复现的优点。

但有限场景可能没有覆盖少数幅度较大的误差，不同随机种子也可能抽到不同的历史日期。现有敏感性结果表明，不同场景数和随机种子下的平均日费用存在一定波动，因此随机场景的构成会影响购电计划的稳定性。

此外，所有历史场景等概率意味着模型没有区分“与当前预测曲线较相似的历史日”和“与当前状态差异较大的历史日”。

#### 可以怎样改进

一种改进是先生成更多候选场景，再从中保留差异较大的代表性场景，检验随着场景数量增加，计划购电量、总费用和紧急购电量是否逐渐稳定。

另一种改进是研究条件场景权重：与当前预测曲线更相似的历史误差可以获得较高权重，同时保留少量差异较大的场景用于覆盖不利情况。这里的相似性应由实际回测确定，不能直接主观规定。

#### 为什么现有模型没有直接采用

场景数量增加后，每个场景都会带来一套储能状态、充放电、紧急购电以及相关二元变量，随机混合整数规划的规模会同步增长。问题三和问题 4-3还需要多次滚动求解，因此不能简单认为场景越多越好。

相似性加权也不是天然正确的。按照负荷曲线、光伏曲线、净负荷曲线还是价格曲线判断相似，可能得到不同的权重；题目没有给出这些概率或相似性标准。现有等概率抽样虽然较简单，但避免了人为指定权重。只有在独立回测证明某种相似性权重能够稳定改善结果后，才适合替换现有方法。

### 4. 当前主要给出单一经济目标下的最优方案

#### 当前做法及其影响

现有模型按照题目给出的正常购电、上调、下调和紧急购电价格计算总费用，并以期望费用最小为主要目标。该方法能够得到一个明确、客观并且直接对应题目计价规则的方案。

但是，费用最低与紧急购电最少并不完全相同。某种方案可能通过减少全天计划购电降低平均费用，却在少数日期产生更多紧急购电；另一种方案可能需要支付略高的正常购电费用，但在不利场景下更加稳定。只报告一个最低期望费用方案，不能完整展示经济性与运行风险之间的关系。

#### 可以怎样改进

后续可以建立费用与风险的双目标模型，分别计算偏重最低费用、偏重减少紧急购电以及二者相对均衡的多组方案。最终不必强行给出唯一答案，而可以展示总费用与紧急购电风险之间的变化关系，供管理者根据实际需求选择。

现有代码已经预留 CVaR 风险项，因此还可以研究在不同风险权重下，高费用场景和紧急购电量如何变化。

#### 为什么现有模型没有直接采用

题目已经给出紧急购电为正常价格 5 倍的费用规则，因此当前模型按照统一货币成本进行优化，能够避免额外引入主观参数。但是，如果建立费用—风险双目标模型，就必须回答“愿意额外支付多少费用来减少多少紧急购电风险”。题目没有给出管理者的风险偏好，也没有规定紧急购电量必须控制在某个水平以下。

在缺少这一管理偏好的情况下直接指定风险权重，得到的结果会依赖人为选择。现有模型先给出严格按照题目价格规则计算的经济方案；多目标模型则适合在实际运营者能够提供风险承受标准时进一步使用。

### 5. 有限优化窗口的末端储电价值采用固定近似

#### 当前做法及其影响

滚动优化只能看到有限长度的未来。若模型完全不考虑计算窗口结束后的运行需求，就可能在窗口末端过度放电，因为模型看不到这些电量在更远未来的价值。

现有模型通过目标储电量和末端偏差惩罚缓解这一问题。问题三使用 1 月份数据比较少量候选参数，并将选定参数冻结后用于正式计算。这种处理能够避免模型在每次运行时随意改变参数，但有限候选值和单月校准仍然只能近似描述全年不同时期的末端储电价值。

#### 可以怎样改进

若未来能够获得其他年份的历史数据，可以使用前一年度或更早数据建立不同季节下的末端储电价值，再在下一年度进行独立检验。也可以通过较长时间范围的优化结果估计“窗口结束时多保留 1 kWh 电量在未来能够减少多少费用”，并将这种估计作为短期滚动模型的末端价值。

#### 为什么现有模型没有直接采用

如果直接使用本年度 2 月至 12 月的真实结果选择终端参数，然后再用同一时期证明该参数效果良好，就相当于提前看到了正式评价期数据，会造成信息泄漏。当前仅使用 1 月份进行校准，是为了将参数选择期和正式结果期分开。

题目只提供有限年度数据，因此无法同时做到“用多个完整季节校准”和“再用完全独立的年度验证”。在没有额外年份数据时，固定且可复现的终端近似比利用正式评价期反复调参更加严谨。

### 6. 事件驱动执行属于对逐十分钟滚动优化的近似

> 本项仅适用于最终正式结果采用事件驱动方案的情况。如果最终使用逐十分钟滚动模型完成全年计算，则本项不应再写成未来改进。

#### 当前做法及其影响

事件驱动方案只在规定的预测或购电调整时刻求解完整随机模型。在相邻事件之间，计划购电量保持不变，储能动作依据最近一次优化得到的参考值和当前真实供需情况执行。

这种方法能够显著减少求解次数，但如果区间内实际情况逐渐偏离原预测，最近一次完整优化得到的储能参考轨迹可能不再是当前状态下最合适的安排。逐十分钟滚动方法能够根据最新储电量和当前实际数据重新优化储能动作，但需要进行数量大得多的混合整数规划求解。

#### 可以怎样改进

后续重点不应写成未经验证的“设置偏差阈值”，而应研究如何降低逐十分钟滚动求解本身的计算成本，例如复用相邻时段相似的模型结构、利用上一时段解作为下一时段求解的初始参考，或者采用适合滚动优化的分解算法。

在比较两种方法时，还应保证初始储电量一致，并处理期末储电量差异。若一种方法费用较低但同时消耗了更多库存电量，不能仅凭当前费用认定其更优。应通过连续多日或全年运行，或者在比较中计入期末剩余电量的价值，得到更公平的结论。

#### 为什么现有模型没有直接采用

代码已经实现逐十分钟滚动求解，因此不存在题意或技术路线上的禁止。现阶段没有把它直接作为默认全年方案，主要是因为一次完整年度需要求解数万次随机混合整数规划，实际运行时间可达到数十小时，并且敏感性分析还需要多次重复。

另一方面，现有局部对照中两种方法的期末储电量差异较大，暂时不能仅凭局部现金费用判断逐十分钟方案在连续运行中一定更优。因此，当前事件驱动方案代表的是可计算性与精细程度之间的明确取舍，而不是把一个已经证明更优的方法无理由留到未来。

## 四、不应写入本文改进部分的内容

### 1. 电池寿命或老化成本

题目没有提供循环寿命、容量衰减、折旧费用或维护成本等数据，无法建立可验证的寿命成本模型。随意添加相关系数会改变原题条件，因此不应作为本文的正式改进建议。

### 2. 在规定时刻判断调整是否值得

现有模型在 6:00、12:00 和 18:00重新优化时，已经把上调费用、下调费用和紧急购电费用同时放入目标函数。保持原计划也是一个允许的方案，因此模型已经会自动判断修改计划是否值得。这是现有功能，不能再次写成未来改进。

### 3. 设置未经验证的偏差阈值触发重算

“预测偏差超过某个数值就重新优化”会引入新的阈值参数。当前没有证据说明应采用绝对误差、相对误差还是费用影响作为触发条件，也没有实验确定合理阈值。因此，该设想暂时不应写成已经可行的改进方案。

### 4. 把补齐全年结果称为未来改进

完整年度结果属于当前结论是否完整的问题，不属于模型方法的未来优化。如果某个问题最终只完成代表日实验，就只能把结论限制在相应代表日范围内，不能用“未来补充全年计算”代替当前应有的证据。

## 五、总体评价

现有模型的主要价值在于：它严格遵守信息到达顺序，完整表达购电调整、紧急购电和储能约束，并通过随机场景和因果回测将计划决策与实际执行联系起来。同时，模型在场景数量、决策阶段和滚动求解频率上采用了较为克制的设计，使结果能够解释、计算和验证。

其后续深化不应依靠随意增加题目没有提供的参数，而应围绕四个方向展开：使计划模型的信息结构更接近真实逐步决策过程，使预测方法的评价与最终调度效果更一致，更准确地表达不同历史场景对当前状态的参考价值，以及展示购电费用与紧急购电风险之间的权衡关系。

其中，多阶段随机规划是最重要的结构性改进，但也是最需要谨慎说明的一项。题目没有直接提供多阶段场景之间的转移关系，强行构造分支时刻和概率会增加主观性；场景树及其非前视约束又会显著扩大计算规模。因此，它适合被表述为在获得更充分信息或形成可靠场景树构造方法后的研究方向，而不应被简单描述成当前模型本来就应该加入却遗漏的步骤。
