# Q1 / Q2 可视化设计与复现

图件读取各题 output/prob*_solution.npz，不运行优化器。每张图由独立模块实现；plot_support.py 仅负责字体、颜色、日期坐标、标注与保存。原有八个 PNG 文件名保持可引用。

## 每张图的选择

| 图 | 图形类型 | 主要信息与标注 |
|---|---|---|
| Q1 图1 | 电价阶梯图 + 负荷/光伏散点 | 电价按 10 分钟区间取值；负荷和光伏不跨区间连线。标注最高/最低电价及光伏峰值。 |
| Q1 图2 | 购电柱 + 净负荷散点 | 同一坐标比较购电与净负荷，橙色点突出光伏富余；标注购电峰值与最大富余。 |
| Q1 图3 | 购电柱与储能正负柱分面 | 保留全部 144 个决策；充电为正、放电为负；标注全日购电、充电、放电总量。 |
| Q1 图4 | 带节点的储能状态轨迹 | E 是累积状态；在区间功率保持不变的模型下，节点间线性变化有物理含义。标注容量边界、初终值与首次触下限时刻，不做平滑。 |
| Q2 图1 | 月度费用堆叠柱 + 每日紧急购电散点 | 月累计费用展示构成，334 个每日散点保留异常事件；标注费用占比、最高费用月和最大紧急购电日。 |
| Q2 图2 | 四日分面购电柱/净负荷散点 + 紧急购电柱 | 计划与净负荷同轴比较；紧急购电单独成面，避免数量级掩盖。四日主图统一范围、四日应急图统一范围，峰谷不截断。 |
| Q2 图3 | 日期×时段热力图 + 小时累计柱 | 原始矩阵每格为一天的一个 10 分钟区间；标注单格最大事件和累计最多的小时。 |
| Q2 图4 | 同时刻跨日分位区间 + 日界电量散点 | 145 个时刻各自统计 334 天的分布，显示 P10—P90、P25—P75 与中位数；日界状态不连线，标注最低值。 |

## 统计与读图约定

- 柱与区间散点置于 10 分钟区间中心，标注中的时刻为该区间起点；储能状态节点置于真实区间边界。
- 计划购电用绿色 #009E73；紧急购电用朱红色 #D55E00；净负荷参考用黑色；光伏用橙色 #E69F00。图例同时使用符号、位置和正负方向表达含义。
- Q2 使用真实日历日期，不用每月 30.44 天近似。月费用是累计值，受月份天数影响，不能直接解释为日均成本。
- 热力图保留完整零值与最大值，采用已在图内注明的平方根色阶，以显示小额应急事件；色条仍标实际 kWh 数值，不截尾、不插值。
- Q2 图4 的分位区间是跨日期的经验分布，不是置信区间，也不是某一天的储能路径；P10—P90 不包含全部极端值。容量约束仍需对完整原始矩阵校验。
- 前日 24:00 与当日 0:00 在当前结果中一致，因此日界散点只显示一次，另补最后一天 24:00。下图纵轴为局部范围，便于识别日界差异。
- 每图仅保留少量峰值、总量或边界标注；所有标注数值由数据计算。

## 复现

在仓库根目录运行：

```powershell
python Q1/src/make_figures.py
python Q2/src/make_figures.py
```

输出为 Q1/output 和 Q2/output 下原名的四张 PNG，180 dpi。每图实现分别位于 Q1/src/figures 和 Q2/src/figures。

## 验证记录

八图生成成功并逐图检查布局。Q1 独立模型校验通过；Q2 的六项单元测试通过，原始可行性、跨日连续性、费用、因果性及紧急购电语义检查通过。现有 Q2/输出 Excel 的紧急购电逐日汇总校验失败；这项表格一致性异常与此次绘图修改独立，图件以 NPZ 为数据源。
